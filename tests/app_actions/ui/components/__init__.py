"""One App Actions class per UI component of the Single Bet Placement page.

Each class owns the intent of a single region of the screen and nothing else.
`BettingAppActions` composes them and keeps only the flows that genuinely span
more than one component.
"""
